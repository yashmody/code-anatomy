{
  "name": "dept-anatomy-cms",
  "version": "1.0.0",
  "private": true,
  "description": "DEPT Tenet — Directus editorial plane (Phase 4a). Stands up over the EXISTING codecoder Postgres by introspecting existing tables. Directus is the content + media + config WRITE plane only; FastAPI remains the runtime read API the SPA + quiz consume.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "bootstrap": "directus bootstrap",
    "start": "directus start",
    "snapshot": "directus schema snapshot ./snapshot.yaml --yes",
    "apply": "directus schema apply ./snapshot.yaml --yes",
    "register": "node ./register-collections.mjs",
    "seed": "bash ./bootstrap.sh"
  },
  "dependencies": {
    "directus": "11.17.4"
  }
}
