{
  "name": "directus-extension-roles-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DEPT Tenet — Directus hook that mirrors staff-role changes to FastAPI POST /api/cms/roles-sync (loopback). One-way Directus -> app; staff roles only (04 §7.2). Phase 4c.",
  "license": "UNLICENSED",
  "directus:extension": {
    "type": "hook",
    "path": "dist/index.js",
    "source": "src/index.ts",
    "host": "^11.17.4"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "directus-extension build",
    "dev": "directus-extension build -w --no-minify"
  },
  "devDependencies": {
    "@directus/extensions-sdk": "^17.1.4",
    "typescript": "^5.9.3"
  }
}
