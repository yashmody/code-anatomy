{
  "name": "directus-extension-media-upload",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "DEPT Tenet — Directus admin module that uploads app media through FastAPI POST /api/media/upload (bytes land in Postgres large objects, never in Directus storage). Phase 4c.",
  "license": "UNLICENSED",
  "directus:extension": {
    "type": "module",
    "path": "dist/index.js",
    "source": "src/index.ts",
    "host": "^11.17.4"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "directus-extension build",
    "dev": "directus-extension build -w --no-minify",
    "link": "directus-extension link"
  },
  "devDependencies": {
    "@directus/extensions-sdk": "^17.1.4",
    "typescript": "^5.9.3",
    "vue": "^3.5.24"
  }
}
