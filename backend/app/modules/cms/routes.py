"""CMS routes — placeholder for Phase 4 (Directus integration).

Today this is an empty APIRouter so main.py can `include_router` it without
404s and Phase 4 can drop endpoints in without touching the composition root.
"""
from fastapi import APIRouter


router = APIRouter()
