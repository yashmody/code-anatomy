"""Pydantic schemas for the media module. Empty for Phase 1 — the upload
endpoint takes a `multipart/form-data` body (FastAPI's UploadFile), and
the streaming endpoints take only a path parameter.
"""
