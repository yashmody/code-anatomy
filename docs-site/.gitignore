# Docusaurus build + generated artefacts — never commit these.
# (node_modules is already covered by the repo-root .gitignore.)

# Build output
/build

# Generated by `docusaurus start` / `docusaurus build`
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
